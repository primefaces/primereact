const CSSTransitionProps = [

];

const CSSTransitionEvents = [

];

const CSSTransitionStyles = [

];

module.exports = {
    csstransition: {
        name: 'CSSTransition',
        description: 'TODO',
        docUrl: 'https://primefaces.org/primereact/showcase/#/csstransition',
        props: CSSTransitionProps,
        events: CSSTransitionEvents,
        styles: CSSTransitionStyles
    }
};