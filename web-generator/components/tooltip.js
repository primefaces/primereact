const TooltipProps = [

];

const TooltipEvents = [

];

const TooltipStyles = [

];

module.exports = {
    tooltip: {
        name: 'Tooltip',
        description: 'TODO',
        docUrl: 'https://primefaces.org/primereact/showcase/#/tooltip',
        props: TooltipProps,
        events: TooltipEvents,
        styles: TooltipStyles
    }
};