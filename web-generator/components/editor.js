const EditorProps = [

];

const EditorEvents = [

];

const EditorStyles = [

];

module.exports = {
    editor: {
        name: 'Editor',
        description: 'TODO',
        docUrl: 'https://primefaces.org/primereact/showcase/#/editor',
        props: EditorProps,
        events: EditorEvents,
        styles: EditorStyles
    }
};