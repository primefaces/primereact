---
title: "MeterGroup"
description: "MeterGroup displays scalar measurements within a known range."
component: "metergroup"
tokens: 
    - "MeterGroup"
styles:
    - "MeterGroup"
pts:
    - "MeterGroup"
---

## Import

```tsx
import { MeterGroup } from 'primereact/metergroup';
```

## Basic 

MeterGroup requires a _value_ as the data to display where each item in the collection should be a type of <i>MeterItem</i>.

<DocComponentViewer name="metergroup-basic-demo" filePath="demo/metergroup/metergroup-basic-demo.tsx"/>

## Multiple

Adding more items to the array displays the meters in a group.

<DocComponentViewer name="metergroup-multiple-demo" filePath="demo/metergroup/metergroup-multiple-demo.tsx"/>

## Icon

Icons can be displayed next to the labels instead of the default marker.

<DocComponentViewer name="metergroup-icon-demo" filePath="demo/metergroup/metergroup-icon-demo.tsx"/>

## Label

The default orientation of the labels is horizontal, and the vertical alternative is available through the _orientation_ option.

<DocComponentViewer name="metergroup-label-demo" filePath="demo/metergroup/metergroup-label-demo.tsx"/>

## Vertical

Layout of the MeterGroup is configured with the _orientation_ property that accepts either _horizontal_ or _vertical_ as available options.

<DocComponentViewer name="metergroup-vertical-demo" filePath="demo/metergroup/metergroup-vertical-demo.tsx"/>

## Min-Max

Boundaries are configured with the _min_ and _max_ values whose defaults are 0 and 100 respectively.

<DocComponentViewer name="metergroup-minmax-demo" filePath="demo/metergroup/metergroup-minmax-demo.tsx"/>

## Template

MeterGroup provides templating support for labels, meter items, and content around the meters.

<DocComponentViewer name="metergroup-template-demo" filePath="demo/metergroup/metergroup-template-demo.tsx"/>

## Accessibility
### Screen Reader

MeterGroup component uses meter role in addition to the aria-valuemin, aria-valuemax and aria-valuenow attributes. Value to describe the component can be defined using aria-labelledby prop.

### Keyboard Support

Component does not include any interactive elements.