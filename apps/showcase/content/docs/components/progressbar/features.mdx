---
title: "ProgressBar"
description: "ProgressBar is a process status indicator."
component: "progressbar"
tokens:
    - "ProgressBar"
styles:
    - "ProgressBar"
pts:
    - "ProgressBar"
---

## Import

```tsx
import { ProgressBar } from 'primereact/progressbar';
```

## Basic

Content to display is defined with the _value_ property or the default slot.

<DocComponentViewer name="progressbar-basic-demo" filePath="demo/progressbar/progressbar-basic-demo.tsx"/>

## Dynamic

Value is reactive so updating it dynamically changes the bar as well.

<DocComponentViewer name="progressbar-dynamic-demo" filePath="demo/progressbar/progressbar-dynamic-demo.tsx"/>

## Template

Content to display is defined with the _value_ property.

<DocComponentViewer name="progressbar-template-demo" filePath="demo/progressbar/progressbar-template-demo.tsx"/>

## Indeterminate

For progresses with no value to track, set the _mode_ property to _indeterminate_.

<DocComponentViewer name="progressbar-indeterminate-demo" filePath="demo/progressbar/progressbar-indeterminate-demo.tsx"/>

## Accessibility

### Screen Reader

ProgressBar components uses progressbar role along with aria-valuemin, aria-valuemax and aria-valuenow attributes. Value to describe the component can be defined using aria-labelledby and aria-label props.

```tsx
<span id="label_status" />
<ProgressBar aria-labelledby="label_status" />

<ProgressBar aria-label="Status" />
```

### Keyboard Support

Not applicable.
