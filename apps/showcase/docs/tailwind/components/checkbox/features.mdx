---
title: Checkbox
description: Checkbox is an extension to standard checkbox element with theming.
component: checkbox
---

<DocDemoViewer name="checkbox:basic-demo" hideCode/>

## Usage

```tsx
import { Checkbox } from '@primereact/ui/checkbox';
```

```tsx
<Checkbox.Group>
    <Checkbox />
</Checkbox.Group>
```

## Examples

### Sizes

Use the `size` property to change the size of a checkbox.

<DocDemoViewer name="checkbox:sizes-demo" />

### Filled

Specify the `filled` property to display the component with a higher visual emphasis than the default outlined style.

<DocDemoViewer name="checkbox:filled-demo" />

### Disabled

Use the `disabled` property to disable a checkbox.

<DocDemoViewer name="checkbox:disabled-demo" />

### Invalid

Specify the `invalid` property to display the component with a red border.

<DocDemoViewer name="checkbox:invalid-demo" />

### Indeterminate

Use the `indeterminate` property to display an indeterminate state.

<DocDemoViewer name="checkbox:indeterminate-demo" />
