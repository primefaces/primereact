---
title: Skeleton
description: Skeleton is a placeholder to display instead of the actual content.
component: skeleton
---

<DocDemoViewer name="skeleton:basic-demo" mode="collapsible" />

## Usage

```tsx
import { Skeleton } from '@primereact/ui/skeleton';
```

```tsx
<Skeleton width="4rem" height="2rem" />
```

## Examples

### Card

Sample Card implementation using different `Skeleton` components and Tailwind CSS utilities.

<DocDemoViewer name="skeleton:card-demo" />

### Shapes

Various shapes and sizes can be created using styling properties like `shape`, `width`, `height`, `size`, `borderRadius` and `className`.

<DocDemoViewer name="skeleton:shapes-demo" />

### Color

Customize the background color of the skeleton.

<DocDemoViewer name="skeleton:color-demo" />

### Grid

Sample Grid implementation using different Skeleton components and Tailwind CSS utilities.

<DocDemoViewer name="skeleton:grid-demo" />

## Accessibility

### Screen Reader

Skeleton uses aria-hidden as "true" so that it gets ignored by screen readers, any valid attribute is passed to the root element so it can be customized further if required. If multiple skeletons are grouped inside a container, aria-busy can be used on the container element as well to indicate the loading process.

### Keyboard Support

Component does not include any interactive elements.
