---
title: Inplace
description: Inplace provides an easy to do editing and display at the same time where clicking the output displays the actual content.
component: inplace
---

<DocDemoViewer name="inplace:basic-demo" mode="collapsible"/>

## Usage

```tsx
import { Inplace } from '@primereact/ui/inplace';
```

```tsx
<Inplace>
    <Inplace.Display></Inplace.Display>
    <Inplace.Content></Inplace.Content>
</Inplace>
```

## Examples

### Disabled

Use the `disabled` prop to disable the inplace content.

<DocDemoViewer name="inplace:disabled-demo" />

### Controlled

Use the `active` and `onActiveChange` props to control the inplace content.

<DocDemoViewer name="inplace:controlled-demo" />

### Image

Any content such as an image can be placed inside the `Inplace.Content` component.

<DocDemoViewer name="inplace:image-demo" />

## Accessibility

### Screen Reader

Inplace component defines _aria-live_ as "polite" by default, since any valid attribute is passed to the main container aria roles and attributes of the root element can be customized easily.

### Keyboard Support

| Key                | Function             |
| ------------------ | -------------------- |
| _enter_ or _space_ | Switches to content. |
