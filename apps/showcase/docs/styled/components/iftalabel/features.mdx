---
title: IftaLabel
description: IftaLabel visually integrates a label with its form element.
component: iftalabel
---

<DocDemoViewer name="iftalabel:basic-demo" mode="collapsible" />

## Usage

```tsx
import { IftaLabel } from '@primereact/ui/iftalabel';
```

```tsx
<IftaLabel>
    {/* input element like InputText, InputNumber, Select, Textarea, input etc. */}
    {/* label element like Label, label etc. */}
</IftaLabel>
```

## Examples

### Invalid

When the form element is invalid, the label is also highlighted.

<DocDemoViewer name="iftalabel:invalid-demo" />

## Accessibility

### Screen Reader

IftaLabel does not require any roles and attributes.

### Keyboard Support

Component does not include any interactive elements.
