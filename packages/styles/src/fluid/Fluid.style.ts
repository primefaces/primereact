import { createStyles } from '@primereact/styles/utils';
import type { FluidInstance } from '@primereact/types/shared/fluid';

export const styles = createStyles<FluidInstance>({
    name: 'fluid',
    classes: {
        root: 'p-fluid'
    }
});
