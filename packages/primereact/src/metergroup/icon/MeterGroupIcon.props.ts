import * as Icon from '@primereact/core/icon';
import type { MeterGroupIconProps } from '@primereact/types/shared/metergroup';

export const defaultIconProps: MeterGroupIconProps = {
    ...Icon.defaultProps
};
