import * as HeadlessContextMenu from '@primereact/headless/contextmenu';
import type { ContextMenuProps } from '@primereact/types/shared/contextmenu';

export const defaultProps: ContextMenuProps = {
    ...HeadlessContextMenu.defaultProps
};
