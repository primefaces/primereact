import * as HeadlessContextMenu from '@primereact/headless/contextmenu';
import type { ContextMenuRootProps } from '@primereact/types/shared/contextmenu';

export const defaultRootProps: ContextMenuRootProps = {
    ...HeadlessContextMenu.defaultProps
};
