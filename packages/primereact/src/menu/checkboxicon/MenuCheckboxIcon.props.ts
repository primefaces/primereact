import * as Icon from '@primereact/core/icon';
import type { MenuCheckboxIconProps } from '@primereact/types/shared/menu';

export const defaultCheckboxIconProps: MenuCheckboxIconProps = {
    ...(Icon.defaultIconProps as MenuCheckboxIconProps)
};
