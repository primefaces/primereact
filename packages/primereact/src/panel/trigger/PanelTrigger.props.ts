import type { PanelTriggerProps } from '@primereact/types/shared/panel';
import { CollapsibleTrigger } from 'primereact/collapsible';

export const defaultTriggerProps: PanelTriggerProps = {
    as: CollapsibleTrigger
};
