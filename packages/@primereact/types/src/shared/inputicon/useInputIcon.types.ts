/**
 *
 * The useInputIcon manages the state and functionality of an input icon component.
 *
 * [Live Demo](https://www.primereact.org/iconfield/)
 *
 * @module useinputicon
 * @group headless
 *
 */
import type { HeadlessInstance } from '@primereact/types/core';

/**
 * Defines valid properties in useInputIcon.
 */
export interface useInputIconProps {}

/**
 * Defines valid state in useInputIcon.
 */
export interface useInputIconState {}

/**
 * Defines the methods and properties exposed by useInputIcon.
 */
export interface useInputIconExposes {}

/**
 * Instance of useInputIcon headless.
 */
export type useInputIconInstance = HeadlessInstance<useInputIconProps, useInputIconState, useInputIconExposes>;
