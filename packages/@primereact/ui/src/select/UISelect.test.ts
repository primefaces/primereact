import { describe, expect, it } from 'vitest';
import { UISelectRoot } from './root';

describe('UISelectRoot', () => {
    it('should be defined', () => {
        expect(UISelectRoot).toBeDefined();
    });
});
