import { createStyles } from '@primereact/styles/utils';
import type { InputIconInstance } from '@primereact/types/shared/inputicon';

export const styles = createStyles<InputIconInstance>({
    name: 'inputicon',
    classes: {
        root: 'p-inputicon'
    }
});
