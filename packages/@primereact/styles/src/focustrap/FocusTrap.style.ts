import { createStyles } from '@primereact/styles/utils';
import type { FocusTrapInstance } from '@primereact/types/shared/focustrap';

export const styles = createStyles<FocusTrapInstance>({
    name: 'focustrap-directive'
});
