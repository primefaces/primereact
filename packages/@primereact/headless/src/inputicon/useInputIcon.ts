import { withHeadless } from '@primereact/core/headless';
import { defaultProps } from './useInputIcon.props';

export const useInputIcon = withHeadless({
    name: 'useInputIcon',
    defaultProps
});
