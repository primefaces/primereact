import { BaseComponentProps } from '..';

/**
 * ToolbarStart component props.
 */
export interface ToolbarStartProps extends BaseComponentProps<{ readonly __TYPE: 'ToolbarStart' }, 'div'> {
    /**
     * The type of the component.
     */
    readonly __TYPE?: 'ToolbarStart';
}
