import { BaseComponentProps } from '..';

/**
 * ToolbarCenter component props.
 */
export interface ToolbarCenterProps extends BaseComponentProps<{ readonly __TYPE: 'ToolbarCenter' }, 'div'> {
    /**
     * The type of the component.
     */
    readonly __TYPE?: 'ToolbarCenter';
}
