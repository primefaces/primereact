import { BaseComponentProps } from '..';

/**
 * ToolbarEnd component props.
 */
export interface ToolbarEndProps extends BaseComponentProps<{ readonly __TYPE: 'ToolbarEnd' }, 'div'> {
    /**
     * The type of the component.
     */
    readonly __TYPE?: 'ToolbarEnd';
}
