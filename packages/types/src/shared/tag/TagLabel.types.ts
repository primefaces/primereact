import { BaseComponentProps } from '..';

/**
 * Tag label component props.
 */
export interface TagLabelProps extends BaseComponentProps<null, 'span'> {
    /**
     * The type of the component.
     */
    readonly __TYPE?: 'TagLabel';
}
