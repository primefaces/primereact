export type CommandItem = { text: string; response?: string };

/**
 * Props for the useTerminal hook.
 */
export interface useTerminalProps {
    /**
     * The type of the hook.
     */
    readonly __TYPE?: 'useTerminal';
}
