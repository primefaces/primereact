import { BaseComponentProps } from '..';

/**
 * CardTitle component props.
 */
export interface CardTitleProps extends BaseComponentProps<{ readonly __TYPE: 'CardTitle' }, 'div'> {
    /**
     * The type of the component.
     */
    readonly __TYPE?: 'CardTitle';
}
