import { BaseComponentProps } from '..';

/**
 * CardHeader component props.
 */
export interface CardHeaderProps extends BaseComponentProps<{ readonly __TYPE: 'CardHeader' }, 'div'> {
    /**
     * The type of the component.
     */
    readonly __TYPE?: 'CardHeader';
}
