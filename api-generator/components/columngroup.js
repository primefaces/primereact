const ColumnGroupProps = [];

const ColumnGroupEvents = [];

const ColumnGroupStyles = [];

module.exports = {
    columngroup: {
        name: 'ColumnGroup',
        description: 'Columns can be grouped at header and footer sections by defining a ColumnGroup component as the headerColumnGroup and footerColumnGroup properties.',
        docUrl: 'https://primefaces.org/primereact/showcase/#/columngroup',
        props: ColumnGroupProps,
        events: ColumnGroupEvents,
        styles: ColumnGroupStyles
    }
};
